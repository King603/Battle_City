export default class {
  x = 0;
  y = 0;
  /**
   * 总类
   * @param {Number} size 尺寸
   * @param {Number} speed 速度
   */
  constructor(size, speed) {
    this.size = size;
    this.speed = speed;
  }
}